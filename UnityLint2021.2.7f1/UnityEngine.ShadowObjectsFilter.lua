--- @class UnityEngine.ShadowObjectsFilter The filters that Unity can use when it renders GameObjects in the shadow pass.
UnityEngine.ShadowObjectsFilter = {
    --- Renders all GameObjects.
    AllObjects = 0,
    --- Only renders GameObjects that do not include the Static Shadow Caster tag.
    DynamicOnly = 1,
    --- Only renders GameObjects that include the Static Shadow Caster tag.
    StaticOnly = 2,
}

---  Generated By xerysherry